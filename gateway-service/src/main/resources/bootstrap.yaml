spring:
  application:
    name: gateway-service
  cloud:
    config:
      enabled: true
      uri:
        - http://localhost:8888

  zipkin:
    base-url: http://127.0.0.1:9411

  sleuth:
    async:
      enabled: true