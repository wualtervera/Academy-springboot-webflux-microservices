server:
  port: 8081
spring:
  application:
    name: ms-course
  cloud:
    config:
      uri:
        - http://localhost:8888 #config server

management:
  endpoints:
    web:
      exposure:
        exclude: "*"
